POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=wishpool
POSTGRES_USER=wishpool
POSTGRES_PASSWORD=wishpool

REDIS_HOST=localhost
REDIS_PORT=6379

CORE_API_BASE_URL=http://localhost:8080
REALTIME_GATEWAY_PORT=8081
WISHPOOL_REALTIME_POLL_INTERVAL_MS=1000
WISHPOOL_REALTIME_HEARTBEAT_INTERVAL_SECONDS=15

S3_ENDPOINT=http://localhost:9000
S3_BUCKET=wishpool-media
S3_ACCESS_KEY=wishpool
S3_SECRET_KEY=wishpool-local
S3_REGION=local

TEMPORAL_ADDRESS=localhost:7233

# Optional image overrides for restricted networks or local mirrors.
MINIO_IMAGE=minio/minio:latest
MINIO_MC_IMAGE=minio/mc:latest
TEMPORAL_IMAGE=temporalio/auto-setup:1.26
TEMPORAL_UI_IMAGE=temporalio/ui:2.38.0

# Optional local port overrides when another project already uses defaults.
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
TEMPORAL_PORT=7233
TEMPORAL_UI_PORT=8088
